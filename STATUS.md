# Release status

Current status: **Release-grade** — the pipeline package, 42 offline unit tests, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/geneformer_single_cell_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The exact committed notebook blob completed its full default path in a clean Kaggle Tesla T4 runtime; `docs/release-verification.md` records the revision, executor, outputs, and bounded sample-sanity results. Unit tests and static conformance checks run in GitHub Actions. Release-grade describes the verified tutorial carrier and does not imply benchmark or production-readiness evidence.
